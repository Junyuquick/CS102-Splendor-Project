# Network UML

ASCII UML-style diagrams for the network package:

- `network-client-ascii.txt`: client-side flow, startup UI, and socket client classes
- `network-server-ascii.txt`: server-side flow, lobby management, and broadcasting
- `network-protocol-ascii.txt`: protocol message structure and message types
